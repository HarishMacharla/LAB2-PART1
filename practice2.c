#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    if (n<0)
        printf("n is less than zero");
    else
    {
        if (n==0)
            printf("n equals to zero");
        else
        {
            printf("n is greater than 0");
        }
        
    }
    
}